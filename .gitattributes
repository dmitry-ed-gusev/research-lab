# -- Set the default behavior, in case people don't have core.autocrlf set
* text=auto

# -- Text files that will be always normalized and converted to native line endings on checkout
# *.java text

# -- Files that will always have LF line endings on checkout.
*.sh text eol=lf
*.py text eol=lf

# -- Files that are truly binary and should not be modified.
*.png binary
*.jpg binary

# -- Dealing with lines endings
#
# https://help.github.com/articles/dealing-with-line-endings/
#
# Linux start script should use lf
/gradlew        text eol=lf

# These are Windows script files and should use crlf
*.bat           text eol=crlf
